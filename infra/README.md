# Infrastructure

Deployment and local development infrastructure will live here.

Initial target:

- Docker Compose
- Backend API
- PostgreSQL
- Redis

The MVP intentionally does not require a local GPU because LLM inference is cloud-API-first.
