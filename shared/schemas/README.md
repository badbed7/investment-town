# Shared Schemas

Cross-layer event and command contracts.

Examples:

- `AgentStatusChanged`
- `TaskStarted`
- `TaskCompleted`
- `ApprovalRequired`
- `TradeProposalCreated`
- `EmergencyStopRequested`

The backend remains the source of truth. Frontends consume these schemas for rendering and control.
